function multiplyArr(arr) {
    let number = 1;
    
    for (let i = 0; i < array.length; i++) {
        number *= array[i];
    }

    console.log(number)
} 

let array = [2 , 5]

multiplyArr(array)